import styles from "./Navbar.module.scss";

const Navbar = () => {
    return (
        <nav className={styles.navbar}>
            <h2>Isis - Gestion des métiers</h2>
        </nav>
    );
};

export default Navbar;
